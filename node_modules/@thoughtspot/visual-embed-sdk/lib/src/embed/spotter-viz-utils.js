export function buildSpotterVizAppInitData(initData, viewConfig) {
    const { spotterViz } = viewConfig;
    if (!spotterViz)
        return initData;
    return {
        ...initData,
        embedParams: {
            ...(initData.embedParams || {}),
            spotterVizConfig: spotterViz,
        },
    };
}
//# sourceMappingURL=spotter-viz-utils.js.map